# Style Guide

In regards to styling, create an application that follows the color schema and style already created. There is a .png file under @./skeleton called `design_style.png`. I do not want to you deviate or make any changes to the style or color schema.